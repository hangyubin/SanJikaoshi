version: '3.8'

services:
  # 数据库服务
  mysql:
    image: mysql:5.7
    container_name: sanjicms-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: smart_exam
      MYSQL_USER: smart_exam_user
      MYSQL_PASSWORD: smart_exam_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - smart-exam-network
  
  # 应用服务 - 使用GHCR远程镜像（前后端合一）
  app:
    image: ghcr.io/hangyubin/sanjicms/app:latest
    container_name: sanjicms-app
    restart: always
    depends_on:
      - mysql
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/smart_exam?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
      SPRING_DATASOURCE_USERNAME: smart_exam_user
      SPRING_DATASOURCE_PASSWORD: smart_exam_password
    ports:
      - "8080:8080"
    networks:
      - smart-exam-network

volumes:
  mysql_data:
    driver: local

networks:
  smart-exam-network:
    driver: bridge
